generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./.dev.db"
}

model User {
  accountId Int @id
  refreshToken String?
  accessToken String?
}